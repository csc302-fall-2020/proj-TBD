import React from 'react';
import styled from 'styled-components';

export type Props = {};

const FormResponseList: React.FC<Props> = () => {
    return <div data-testid="form-response-list-page">This page is where you see and search for form responses</div>;
};

export default FormResponseList;
