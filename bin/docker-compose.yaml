version: '3.7'
services:
    client:
        image: tomac7/name-tbd:frontend
        tty: true
        ports:
            - 3000:3000
        volumes:
            - ${CODE_DIR}/frontend/src:/app/src
            - ${CODE_DIR}/frontend/public:/app/public
        working_dir: /app
        command: ["/usr/local/bin/npm", "start"]
